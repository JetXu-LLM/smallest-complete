interface:
  display_name: "Smallest Complete"
  short_description: "Complete and validate the working result"
  default_prompt: "Use $smallest-complete to execute the settled contract, validate the actual result, and stop at the authorized boundary."
